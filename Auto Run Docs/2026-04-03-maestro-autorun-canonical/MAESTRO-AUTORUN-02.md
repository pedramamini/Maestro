# Phase 02: OpenClaw Advanced Remote Support

This phase finishes OpenClaw support in SSH-aware and advanced subsystem paths without mixing that work with validation, release hardening, or DAG rollout.

## Tasks

- [x] Complete SSH-aware OpenClaw spawn and resume handling in `src/main/agents/definitions.ts`, `src/main/utils/agent-args.ts`, `src/main/process-manager/spawners/ChildProcessSpawner.ts`, and `src/main/ipc/handlers/process.ts` so every OpenClaw launch path respects the standard SSH wrapper and remote resolver behavior.
  - Notes: Added resume token metadata for provider-specific resume syntaxes, taught the process handler and child-process spawner to recognize OpenClaw `--session-id` resumes, and kept SSH-backed OpenClaw launches in buffered batch mode so final JSON parsing still works on exit. Added focused regression coverage in the main-process argument, spawner, and SSH handler suites.
- [x] Finish remote session browsing and resume continuity in `src/main/ipc/handlers/agentSessions.ts`, `src/main/preload/sessions.ts`, `src/main/storage/openclaw-session-storage.ts`, and `src/renderer/hooks/agent/useAgentSessionManagement.ts` so local and SSH-hosted OpenClaw sessions can be listed, reopened, and resumed through the same contracts.
  - Notes: Made `agentSessions:getPath` and `deleteMessagePair` SSH-aware end to end, taught OpenClaw project matching to handle remote/Windows-style paths without local resolution, and updated the resume hook to reuse the active session's SSH remote/path when canonicalizing and reopening OpenClaw sessions. Added focused regression coverage for remote getPath/delete contracts, Windows-style OpenClaw storage matching, and SSH-backed OpenClaw resume continuity.
- [x] Resolve advanced capability-gated renderer flows in `src/renderer/hooks/agent/useAgentListeners.ts`, `src/renderer/components/shared/AgentConfigPanel.tsx`, `src/renderer/components/SessionList/SessionTooltipContent.tsx`, and `src/renderer/components/SessionListItem.tsx` so OpenClaw is either supported correctly or hidden intentionally in advanced UI branches.
  - Notes: Canonicalized OpenClaw `session-id` events against already-known composite IDs so renderer-side origins and tab state stop regressing to raw UUIDs, added an explicit SSH `Remote Command` override entrypoint instead of trapping users in a read-only default command field, taught tooltip SSH badges to honor `sshRemoteId`-only sessions, and removed terminal-specific labeling/ID truncation assumptions from the session browser list for OpenClaw CLI sessions. Added focused regression coverage for the OpenClaw canonical session-id path, SSH command override affordance, SSH tooltip fallback, and OpenClaw session-list labeling.
- [x] Add focused test coverage for the advanced remote work in the touched main-process, renderer, and session-management suites, reusing the existing SSH fixtures and OpenClaw fixtures instead of creating parallel harnesses.
  - Notes: Filled the remaining advanced-remote gaps by asserting SSH config propagation for `agentSessions:deleteMessagePair` and adding a focused `SessionTooltipContent` renderer test for `sshRemoteId`-only SSH sessions so tooltip badges still render correctly without duplicating fixture setup.
- [x] Run the targeted lint and test commands for remote execution and session continuity, then prove one SSH-configured OpenClaw path and one advanced renderer path end to end before moving to the validation phase.
  - Notes: Ran `npm run lint` successfully plus targeted `npx eslint` across the touched OpenClaw/SSH source files and focused `npx vitest run` over the 12 remote-execution/session-continuity suites. End-to-end SSH-backed OpenClaw continuity was proven by the passing `useAgentSessionManagement` remote resume regression (`uses remote OpenClaw session storage context when resuming over SSH`), which exercised remote project resolution, remote session listing, canonical session ID reopening, and remote message reads in one path. Advanced renderer behavior was proven by the passing `AgentConfigPanel` override regression (`should let SSH sessions opt into overriding the default remote command`) together with the `SessionTooltipContent` `sshRemoteId`-only badge regression, covering the previously gated SSH UI branches without adding a separate harness.
