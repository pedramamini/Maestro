2026-04-03: Canonical current truth is now frozen in `Auto Run Docs/2026-04-03-maestro-autorun-canonical/`. The existing uncommitted diff already spans baseline metadata/runtime work plus partial Phase 09 analytics UI touches, so use the canonical docs as the source of truth before editing those files again.
2026-04-03: Phase 01 shared playbook contract alignment is complete. `src/shared/types.ts` now owns the baseline metadata, worktree settings, draft, and update types; `src/main/preload/autorun.ts` and `src/shared/marketplace-types.ts` now alias those shared shapes instead of re-declaring them.
